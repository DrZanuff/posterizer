import { createTheme } from '@mui/material/styles'

export const themeOptions = createTheme({
  palette: {
    primary: {
      main: '#ff8000',
    },
    secondary: {
      main: '#0080ff',
    },
  },
})
